module.exports = {
    presets: [
        // env -> envoiment -> ambiente
        '@babel/preset-env', // converte o código do JS mais antigo para um mais moderno, baseano no ambiente de desenvolvimento
        '@babel/preset-react' // adiciona as funcionalidades do react a conversão
    ]
}